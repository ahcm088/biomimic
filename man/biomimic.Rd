% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lavaan_models.R
\name{biomimic}
\alias{biomimic}
\title{Run the Full biomimic Pipeline}
\usage{
biomimic(
  Y,
  X,
  K = 1L,
  k = 15L,
  model_type = "mimic",
  estimator = "MLR",
  group_var = NULL,
  indicator_labels = NULL,
  factor_labels = NULL,
  ...
)
}
\arguments{
\item{Y}{Numeric matrix (n x p) of observed variables.}

\item{X}{Numeric matrix (n x q) of covariates (should include intercept).}

\item{K}{Integer, number of latent factors (default 1).}

\item{k}{Integer, panel size for variable selection (default 15).}

\item{model_type}{Character, Stage 3 model type (default \code{"mimic"}).}

\item{estimator}{Character, lavaan estimator (default \code{"MLR"}).}

\item{group_var}{Character, grouping variable (for multigroup models).}

\item{indicator_labels}{Named character vector mapping variable names
to biology-friendly labels. If \code{NULL}, original names are used.}

\item{factor_labels}{Character vector of factor names. If \code{NULL},
defaults are generated.}

\item{...}{Additional arguments passed to \code{vb_ard()}.}
}
\value{
A \code{biomimic_fit} object. The VB-ARD fit is accessible via
\code{$selection$fit}.
}
\description{
Convenience function that runs all three stages: VB-ARD screening,
top-k selection, and lavaan estimation. Returns a \code{biomimic_fit}
object with the VB-ARD fit attached.
}
